import { restartGame } from './finished';

export const MAIN_SCREEN = document.querySelector('.mainScreen');
export const FIN_SCREEN = document.querySelector('.finishedScreen');

export const routes = {
  'main': '/',
  'finished': '/finished'
};
